# Test fixtures

Only synthetic, redistributable fixtures may be committed here. Every fixture
must document its generator, expected behavior, license, and security purpose.

Do not commit customer files, credentials, personal data, or malware samples.
Large fuzzing corpora should be stored as CI artifacts or in a dedicated test
data release rather than in normal source history.

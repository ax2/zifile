name: Publish documentation

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - docs/**
      - packaging/store/listing.*.json
      - packaging/store/Test-PublicPrivacy.ps1
      - ROADMAP.md
      - .github/workflows/docs-pages.yml

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 10.3.0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: docs/pnpm-lock.yaml
      - run: pnpm --dir docs install --frozen-lockfile
      - run: pnpm --dir docs build
      - name: Validate generated Store privacy routes
        shell: pwsh
        run: ./packaging/store/Test-PublicPrivacy.ps1 -DocumentationOutput ./docs/dist
      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
      - uses: actions/checkout@v7
      - name: Validate deployed Store privacy routes
        shell: pwsh
        run: ./packaging/store/Test-PublicPrivacy.ps1 -Live
